# Unfold AI 2.0 Release Evidence

Date: 2026-09-04

## Automated gates

- `npm run lint`: zero errors and zero warnings.
- `npm run type-check`: zero TypeScript errors.
- `npm test`: 53 tests across document modeling, summary engines, Answer Map, Saved insights, runtime messages, providers, Shadow DOM UI, and lifecycle behavior.
- `npm run build`: Chrome Manifest V3 build completed.
- `npm run build:firefox`: Firefox Manifest V2 build completed with its browser-specific manifest installed.
- `npm run validate:package`: both allowlisted release archives passed structure, version, manifest, size, and forbidden-file checks.
- `npm audit`: zero known vulnerabilities across runtime and development dependencies after the repository-hygiene update; the extension has no runtime npm dependencies.

## Browser evidence

- Chromium store preview reviewed at 1280×800 with zero console errors or warnings.
- Compiled Chrome build loaded in a temporary headless Chrome profile without a manifest or extension-load failure. Chrome emitted macOS headless display-service diagnostics, unrelated to the extension.
- Compiled Firefox onboarding and popup inspected in Firefox at desktop and 375px widths with zero console errors or warnings.
- Mozilla `web-ext lint --self-hosted`: zero errors, zero notices, zero warnings.
- Firefox compatibility is declared as desktop 140+ and Android 142+ to support the current built-in no-data-collection declaration.

## Store assets

- Five screenshots: 1280×800 PNG.
- Small promotional image: 440×280 PNG.
- Marquee promotional image: 1400×560 PNG.
- Official folded “U” icon master: 1254×1254 PNG with rounded alpha corners and a sunrise-coral contrast field.
- Chrome and Firefox packages contain validated 16, 32, 48, and 128 px derivatives; a 512 px derivative is retained for high-resolution surfaces.
- UI screenshots are deterministic renders of the production Answer Map component using synthetic answer content. All screenshots and promotional images use the same official icon.

## Release boundary

- Chrome archive: `release/unfold-ai-chrome-v2.0.0.zip`.
- Firefox archive: `release/unfold-ai-firefox-v2.0.0.zip`.
- Both archives contain 25 allowlisted production files and exclude previews, screenshots, source maps, tests, documentation, fixtures, secrets, and dependencies.
- Upload, store metadata entry, signing, and publication remain founder-controlled actions.

## Public repository audit

- GitHub secret scanning and push protection are enabled with zero open secret alerts on 2026-09-04.
- An independent scan covered 21 commits and 209 unique blobs reachable from all three public branches.
- No API keys, access tokens, passwords, private keys, JWTs, credential-bearing URLs, or suspicious high-entropy strings were found.
- The only content indicators were a deliberate `test@example.com` safety fixture and two obsolete local build paths in early v1 history; neither is a credential.
- Internal product metrics, growth targets, competitive notes, and agent execution plans were removed from the current public tree.
- The README uses the existing 512 px icon derivative instead of downloading the 1.9 MB source master for a 116 px display.

## Remaining human checks

Authenticated live-site smoke checks remain necessary for all seven providers because their private chat DOM cannot be fully established by deterministic fixtures. A provider selector change is a compatibility issue, not evidence of a core-engine failure.
