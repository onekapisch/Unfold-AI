{
  "name": "layered-ai-reader",
  "version": "2.0.0",
  "description": "Private cross-browser extension that summarizes and maps long AI answers.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite build --watch --mode development",
    "lint": "eslint src scripts vite*.ts --max-warnings=0",
    "build": "tsc --noEmit && vite build && vite build --config vite.content.config.ts",
    "build:dev": "NODE_ENV=development tsc --noEmit && NODE_ENV=development vite build && NODE_ENV=development vite build --config vite.content.config.ts",
    "typecheck": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "package": "npm run build && npm run package:chrome",
    "package:chrome": "node scripts/package.mjs",
    "build:firefox": "tsc --noEmit && vite build --config vite.firefox.config.ts && vite build --config vite.firefox.content.config.ts && node scripts/install-firefox-manifest.mjs",
    "package:firefox": "npm run build:firefox && npm run package:firefox-only",
    "package:firefox-only": "node scripts/package-firefox.mjs",
    "build:preview": "vite build --config vite.preview.config.ts",
    "validate:package": "node scripts/validate-package.mjs",
    "capture:store": "npm run build:preview && node scripts/capture-store-assets.mjs",
    "verify": "npm run lint && npm run type-check && npm test && npm run build && npm run package:chrome && npm run build:firefox && npm run package:firefox-only && npm run validate:package"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/chrome": "^0.2.7",
    "@types/jsdom": "^28.0.3",
    "eslint": "^10.9.1",
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^29.1.1",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.69.0",
    "vite": "^6.4.3",
    "vitest": "^3.2.7"
  }
}
